line1：介绍语
line2：while条件循环，判断为True直接执行下面指令
line3：模式介绍
line4：获取输入并存入choice变量
line5：如果用户输入5，则执行line6、7
line6、7：输出退出提示语并强行跳出循环
line8、9、10：如果玩家输入的数字不在1到4之间，就输出提示语并结束本次循环，进入下一次循环
line11、30：尝试运行try命令下的程序，如果发生（ValueError）就执行except下的代码以代替报错终止运行。此处用于防止用户输入非数字导致int/float失败。
line12、13：获取计算的两个数字并转化为浮点数格式计算（浮点数同时兼容整数运算和小数运算）
line14、15、16：判断用户的选择的模式（choice变量），将输入的两个数字（num1变量和num2变量），并根据模式将对应的运算符存入变量（op）
line17、18、19,20、21、22：同上
line23~28：因为经过上述运算，choice的值只可能是4进行除法运算，所以用else（否则），为了避免除数为0，用if进行检查，如果为0就输出提示并结束本次循环，进入下一次循环，并根据模式将对应的运算符存入变量op
line29：将使用f直接在输出的字符串中调用变量，输出算式及结果。使用:.2f控制小数位数为2
line31：输出替代报错的文字